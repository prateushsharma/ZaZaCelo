{
  "name": "@agentmesh/payment-adapter",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint src",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agentmesh/types": "workspace:*",
    "@agentmesh/logger": "workspace:*"
  },
  "devDependencies": {
    "@agentmesh/tsconfig": "workspace:*",
    "@agentmesh/eslint-config": "workspace:*",
    "typescript": "^5.6.3"
  }
}
