{
  "name": "@agentmesh/messaging-node",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "dev": "cargo run",
    "test": "cargo test",
    "lint": "cargo clippy -- -D warnings",
    "clean": "cargo clean"
  }
}
