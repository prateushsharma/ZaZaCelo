{
  "name": "@agentmesh/dashboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "lint": "eslint src",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agentmesh/types": "workspace:*"
  },
  "devDependencies": {
    "@agentmesh/tsconfig": "workspace:*",
    "@agentmesh/eslint-config": "workspace:*",
    "typescript": "^5.6.3"
  }
}
